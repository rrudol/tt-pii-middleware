# Contributing

1. Fork & branch from `main`.
2. `make install && make test && make eval-public`
3. Keep examples/tests **synthetic only** — never commit real PII.
4. Open a PR; CI must be green.
5. By contributing you agree your changes are Apache-2.0 (see LICENSE).
